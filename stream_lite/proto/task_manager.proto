syntax = "proto2";

import "stream_lite/proto/common.proto";

package task_manager;


message MessageRequest {
  optional string logid = 1;
};


service TaskManagerService {
  // from task to task
  rpc push_message(MessageRequest) returns (common.NilResponse) {}
};
